# Tween 1.0.1 — resubmission after the 2026-08-31 rejection

Submission ID `92a60275-bf2b-4541-a165-e0e7c0e4fc31`, reviewed on an iPad Air
11-inch (M3) / iPadOS 26.6.1 against build **1.0.1 (401)**.

Three findings, **one root cause**: the two in-app purchases were never
attached to the submission. Unsubmitted products don't vend, so the paywall
had nothing to show — which App Review reported separately as a broken page.

| # | Guideline | What Apple said | Who fixes it |
|---|---|---|---|
| 1 | 2.1(b) | "the Tween Pro page did not load properly" | symptom of #2, plus code (done, 27c47a8) |
| 2 | 2.1(b) | IAP products not submitted for review | **App Store Connect — human only** |
| 3 | 5.2.5 | "iMessage" in the app subtitle | **App Store Connect — human only** |

## Already done in the repo (27c47a8, on `main`, pushed)

- Paywall retries the StoreKit load (1s, 3s) before showing any failure, and
  reloads when the app returns to the foreground.
- A thrown error and a clean empty answer are told apart. Empty no longer
  claims "The App Store isn't reachable"; it says the plans aren't available
  from this account yet.
- Comparison row "Share and agree inside iMessage" → "Share and agree right
  in your chat" (same 5.2.5 phrasing as the subtitle, on the screen App
  Review photographed).
- `metadata/subtitle.txt` → "Nobody drives the long way" (26 chars).

Pushing `main` triggered Codemagic (`ios-testflight`). It uploads to
TestFlight and does **not** submit for review — the submit is still yours.

## Verified in App Store Connect on 2026-08-31 (and what was done)

Done automatically:

- **Subtitle** → "Nobody drives the long way". (The live value was "Fair meetup
  spots in iMessage", not the "Fair meetups in iMessage" this repo recorded.)
- **Subscription group "Tween Pro" added to the draft submission** — it is now
  Ready for Review, and the draft holds 2 items (Monthly + the group).
- **Build attached to version 1.0.1**, replacing the rejected 401. The version
  moved from Rejected to Prepare for Submission. Currently **407**.

**Before submitting, confirm the attached build actually contains the finished
paywall fix.** Codemagic publishes no release notes, so a TestFlight build
carries no commit marker and ASC cannot tell you which commit it came from.
Build 407 uploaded at 11:52, ~25 min after `843c562` (the incomplete first fix)
and ~2 min after `4c69470` — by timing it is almost certainly `843c562`. The
complete fix is `60d6b7f`. Take the newest build that finished at least ~20
minutes after a push containing `60d6b7f`, or check the Codemagic dashboard,
which does show the commit per build.

Worth fixing once: add release notes carrying `$CM_COMMIT` to the
`publishing.app_store_connect` block in `codemagic.yaml`, so every future
TestFlight build says which commit it is. Note `cancel_previous_builds: true`
(codemagic.yaml:12) means a second push while a build is running kills the
first — which is why build 406 never appeared.

Found, and blocking:

- **There is NO Paid Apps agreement.** Business → Agreements lists only "Free
  Apps Agreement — Active". This is the root cause of the empty product list:
  without it StoreKit vends nothing, so the paywall cannot load in review no
  matter what else is fixed. Account Holder (Kavi) only; needs bank + tax.
- **The Apple Developer Program License Agreement has been updated and must be
  accepted** before existing apps can be updated or submitted. Account Holder
  only. This blocks submission on its own.
- **EU Digital Services Act trader status is still unanswered** — the same
  banner flagged back in SUBMIT.md. Admin/Account Holder only.
- **Tween Pro Lifetime cannot be added for review**: ASC answers "You must add
  a Review Information screenshot." Its screenshot slot is empty; the notes
  are filled. Adding it is otherwise ready.

Screenshot upload could not be automated — ASC's file input accepts a
programmatic file but never validates or saves it (four attempts, nothing
persisted through a reload). Upload `submission/iap-review-screenshot.png`
(1320x2868, alpha flattened - the original had an alpha channel, which Apple
rejects) by hand, then Add for Review on the lifetime product.

## Steps in App Store Connect, in order

### 1. Business → Agreements — CONFIRMED MISSING, do this first
There is no Paid Apps agreement on the account, only the Free Apps one. Kavi
(Account Holder) has to request/accept it and complete bank + tax details.
While it is absent StoreKit vends nothing, and the paywall fails review again
for exactly the reason it just did. Accept the updated Developer Program
License Agreement on the same page, and answer the DSA trader question.

### 2. Verify the products vend, before resubmitting
Install the new build from TestFlight and open Settings → Tween Pro.

- Prices shown → the store side is healthy; go to step 3.
- "Pro plans aren't available from this App Store account yet" → the store
  is still not vending. Do NOT resubmit; fix steps 1/3 first. This is the
  reviewer's exact failure, now stated honestly.
- "The App Store isn't reachable right now" → network only; retry.

### 3. Upload the lifetime IAP's review screenshot, then add it
In-App Purchases → Tween Pro Lifetime → Review Information → Choose File →
`submission/iap-review-screenshot.png` → Save → Add for Review → Draft
Submission. Monthly and the subscription group are already staged.

### 3b. Reference: both IAPs must read "Ready to Submit"
`com.kavigandham.TweenApp.pro.lifetime` ($9.99, non-consumable) and
`com.kavigandham.TweenApp.pro.monthly` ($1.99/mo, auto-renewable). Each needs
the review screenshot (`submission/paywall-review-screenshot.png`) and review
notes. A missing review screenshot alone blocks submission.

### 4. Subtitle — DONE

Left as a judgment call: `metadata/keywords.txt` still contains `imessage`,
and the description still says "Works right inside iMessage". Apple flagged
only the subtitle, and both are referential compatibility uses — but they are
the same guideline if review gets stricter.

### 5. Attach BOTH IAPs and the new build, then resubmit
Build 405 is already attached. Once the agreement is in effect and the
lifetime screenshot is uploaded, add the version to the same submission and
**Submit for Review**.

(The earlier note that this attach flow ignores automated clicks is out of
date — the subscription group attached cleanly. File UPLOADS are what cannot
be automated.)

Version stays **1.0.1**: 401 was rejected, never released, so the version
string is reusable.

## Reply to App Review

> Thank you for the detailed review.
>
> 2.1(b) — In-App Purchase products not submitted: both products
> (com.kavigandham.TweenApp.pro.lifetime and
> com.kavigandham.TweenApp.pro.monthly) and the "Tween Pro" subscription
> group are attached to this submission, and the Paid Apps agreement is now
> in effect, so the products load in the sandbox.
>
> 2.1(b) — "Tween Pro" page: the page itself rendered correctly; StoreKit
> returned no products because the purchases had not been submitted, and the
> screen then reported it as a connectivity problem. This build retries the
> product load before reporting any failure and distinguishes a network error
> from an empty product list, so the message matches what actually happened.
>
> 5.2.5 — The app subtitle no longer refers to iMessage, and the same wording
> has been removed from the in-app feature list.
>
> To exercise Pro: open the Tween app, tap the "..." button at the top right
> of the map, choose Settings, then "Tween Pro" to reach the purchase screen.

That walkthrough matches the shipping build: the "..." menu in
`OnboardingView+BottomSheet.swift` holds the Settings item, and the Pro row in
`SettingsSheet.swift` is present whether or not Pro is already unlocked.
