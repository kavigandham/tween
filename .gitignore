# Secrets — never commit App Store Connect keys or signing keys
.mcp.json
*.p8
AuthKey_*.p8
*.pem
*.p12
# Holds the live Google Places key. The rule for it lives on the 1.1 branch;
# without it here, the repo's own `git add -A` convention commits the key.
Secrets.xcconfig

# Xcode / build artifacts
build/
DerivedData/
*.xcarchive
*.ipa
*.dSYM.zip
*.xcuserstate
xcuserdata/

# macOS
.DS_Store
